/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'links.papareact.com', 'platform-lookaside.fbsbx.com', 'firebasestorage.googleapis.com'],
    }
}

module.exports = nextConfig
